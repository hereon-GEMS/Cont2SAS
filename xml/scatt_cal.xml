<!-- 
input xml for scattering calculation
list params:
(maintain the order)
1) discretization - discretization
    1a) number of categories - num_cat
    1b) method of categorization - method_cat
2) params for calculating scattering function - scatt_cal
    2a) name of signal file (sassena output)- sig_file
    2b) scan vector (along which Q vectors will be calculated) - scan_vec
    2c) lower bound of Q - Q_start
    2d) upper bound of Q - Q_end
    2e) number of Q points - num_points
    2f) number of Q oriantation (orientational average) - num_orientation
-->

<root>
    <discretization>
        <num_cat>101</num_cat>
        <method_cat>extend</method_cat>
    </discretization>
    <scatt_cal>
        <sig_file>signal.h5</sig_file>
        <scan_vec>
            <x>1</x>
            <y>0</y>
            <z>0</z>
        </scan_vec>
        <Q_start>0.0</Q_start>
        <Q_end>1.0</Q_end>
        <num_points>100</num_points>
        <num_orientation>100</num_orientation>
    </scatt_cal>
</root>